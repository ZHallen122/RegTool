apiVersion: 1

datasources:
  - name: Prometheus
    # The dashboard refers to the datasource by uid, so this must not change.
    uid: regtool-prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
