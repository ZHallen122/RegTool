# Prometheus Operator scraping, with the selector label a kube-prometheus-stack
# install would be looking for.
serviceMonitor:
  enabled: true
  interval: 60s
  scrapeTimeout: 15s
  labels:
    release: kube-prometheus-stack
